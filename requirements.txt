# Core VISA communication
pyvisa>=1.13.0

# Pure-Python VISA backend
pyvisa-py>=0.7.2

# USB-TMC instrument support (required by pyvisa-py)
# Windows: also run Zadig (zadig.akeo.ie) once per instrument to install WinUSB
pyusb>=1.2.1
libusb-package>=1.0.26.1

# RS-232/RS-422 serial instrument support (required by pyvisa-py)
pyserial>=3.5

# Test framework
pytest>=7.4.0
